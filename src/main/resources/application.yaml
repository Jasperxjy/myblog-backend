image:
  root:
    path: /usr/blog/image
  default:
    album:
      id: d0d9e5f1e39843b09ed79fc435051d8d
music:
  root:
    path: /usr/blog/music

server:
  port: 8082
  tomcat:
    max-swallow-size: -1
    max-http-form-post-size: -1
    connection-timeout: 120000  # 120秒
jwt:
  secret: ghbsgasrfqwe_fsdfasdfasqwer12345sdf
  expiration: 5400
spring:
  mvc:
    async:
      request-timeout: 120000  # 120秒
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=Asia/Shanghai&useUnicode=true&useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf-8
    username: root
    password: bl.264979
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd HH:mm:ss
    serialization:
      write-date-keys-as-timestamps: false
  cache:
    redis:
      time-to-live: 3600
  data:
    redis:
      host: localhost
      password: jasperpasswordforredis264979
      port: 6379
      timeout: 2000


logging:
  level:
    root: info
